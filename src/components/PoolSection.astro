---
import { Picture } from "astro:assets";
import droneImage from "../assets/drone-overview.jpg";
---

<section class="py-16 md:py-24 bg-background">
  <div class="container mx-auto px-4">
    <div class="grid lg:grid-cols-2 gap-8 lg:gap-12 items-center">
      <!-- Image -->
      <div class="relative rounded-xl overflow-hidden shadow-xl">
        <Picture
          src={droneImage}
          alt="Luftaufnahme der Wohnanlage Casa Cactus in Sagres mit Pool und Gemeinschaftsanlage" 
          formats={["avif", "webp"]}
          widths={[480, 640, 768, 960, 1280]}
          sizes="(min-width: 1400px) 628px, (min-width: 1024px) calc((100vw - 9rem) / 2), calc(100vw - 6rem)"
          quality={50}
          class="w-full h-auto aspect-square object-cover"
          loading="lazy"
        />
      </div>
      
      <!-- Content -->
      <div>
        <h2 class="text-2xl sm:text-3xl md:text-4xl font-bold text-foreground mb-6">
          Gemeinschaftspool und Terrasse
        </h2>
        
        <p class="text-muted-foreground text-lg mb-6 leading-relaxed">
          Der großzügige Gemeinschaftspool steht ausschließlich den Bewohnern der Anlage zur 
          Verfügung. Von der privaten Terrasse aus haben Sie direkten Zugang zum Poolbereich. 
          Sonnenliegen sind vorhanden, Poolhandtücher werden gestellt.
        </p>
        
        <p class="text-foreground text-lg font-medium italic">
          Ideal für entspannte Tage nach dem Strand oder zum Abschalten ohne Trubel.
        </p>
      </div>
    </div>
  </div>
</section>
